<!DOCTYPE node PUBLIC "-//freedesktop//DTD D-BUS Object Introspection 1.0//EN"
  "http://www.freedesktop.org/standards/dbus/1.0/introspect.dtd">
<!--
  The Synara KWin computer-use plugin, service org.synara.ComputerUse.

  This file is the declaration of the interface the plugin exports with
  QDBusConnection::ExportAllInvokables; tests/introspection_test.py holds it
  against the Q_INVOKABLE declarations in synaracomputeruseplugin.h, and the
  server's kwinPluginInterface.test.ts holds its proxy against this file.

  Every method but authenticate and healthJson answers
  org.synara.ComputerUse.Error.Unauthorized until the calling connection has
  authenticated. Other errors a method can return, all retryable on the server
  side unless noted:
    org.synara.ComputerUse.Error.Throttled       authenticate: a failed attempt from
                                                 this connection is under a second old
    org.synara.ComputerUse.Error.SessionLocked   screen locked or session inactive
    org.synara.ComputerUse.Error.HumanActive     the human is using the target
    org.synara.ComputerUse.Error.SeatUnsupported the client holds no input object
    org.synara.ComputerUse.Error.ControlReleased latched by the panic shortcut
    org.synara.ComputerUse.Error.CaptureFailed   a capture could not be rendered

  Interface versions. healthJson carries "interfaceVersion" (2 for this file;
  absent means 1) and "features", the optional methods below the version 1
  set that this build implements. A server calls one only when its feature is
  listed and falls back to the version 1 methods otherwise:
    captureEx          captureWindowEx, captureRegionEx
    keys               keys
    waitForSettle      waitForSettle
    windowsStateJson   windowsStateJson
-->
<node name="/org/synara/ComputerUse">
  <interface name="org.synara.ComputerUse1">
    <!-- Binds the capability to the calling connection; empty on refusal. -->
    <method name="authenticate">
      <arg name="token" type="s" direction="in"/>
      <arg name="instance" type="s" direction="out"/>
    </method>

    <!-- Public diagnostics (no pixels, titles or input); the server's poll. -->
    <method name="healthJson">
      <arg name="json" type="s" direction="out"/>
    </method>
    <method name="stateJson">
      <arg name="json" type="s" direction="out"/>
    </method>
    <method name="windowsJson">
      <arg name="json" type="s" direction="out"/>
    </method>
    <!-- Feature windowsStateJson: {"windows": <windowsJson's array>,
         "targetWindowId": <id or null>, "workspace": {x, y, width, height},
         "outputs": [{x, y, width, height}, ...], "locked": <bool>} in one
         call. Locked, it answers with no windows and a null target instead
         of refusing. -->
    <method name="windowsStateJson">
      <arg name="json" type="s" direction="out"/>
    </method>

    <!-- Session lifetime. -->
    <method name="start">
      <arg name="started" type="b" direction="out"/>
    </method>
    <method name="stop">
      <arg name="stopped" type="b" direction="out"/>
    </method>
    <method name="setIdleTimeout">
      <arg name="milliseconds" type="u" direction="in"/>
      <arg name="accepted" type="b" direction="out"/>
    </method>
    <method name="setHumanActiveGuardMs">
      <arg name="milliseconds" type="u" direction="in"/>
      <arg name="accepted" type="b" direction="out"/>
    </method>
    <method name="setAgentName">
      <arg name="name" type="s" direction="in"/>
      <arg name="accepted" type="b" direction="out"/>
    </method>

    <!-- Targeting. -->
    <method name="focusWindow">
      <arg name="windowId" type="s" direction="in"/>
      <arg name="focused" type="b" direction="out"/>
    </method>
    <method name="raiseWindow">
      <arg name="windowId" type="s" direction="in"/>
      <arg name="raised" type="b" direction="out"/>
    </method>
    <method name="clearFocusWindow">
      <arg name="cleared" type="b" direction="out"/>
    </method>
    <!-- Lease handover: clears the target, releases held input, hands every
         shared seat0 object back to the human. Works while stopped. -->
    <method name="resetInputDelivery">
      <arg name="reset" type="b" direction="out"/>
    </method>

    <!-- Input. Coordinates and deltas are desktop pixels. -->
    <method name="movePointer">
      <arg name="x" type="d" direction="in"/>
      <arg name="y" type="d" direction="in"/>
      <arg name="delivered" type="b" direction="out"/>
    </method>
    <method name="button">
      <arg name="button" type="u" direction="in"/>
      <arg name="pressed" type="b" direction="in"/>
      <arg name="delivered" type="b" direction="out"/>
    </method>
    <method name="axis">
      <arg name="horizontal" type="d" direction="in"/>
      <arg name="vertical" type="d" direction="in"/>
      <arg name="delivered" type="b" direction="out"/>
    </method>
    <method name="key">
      <arg name="keyCode" type="u" direction="in"/>
      <arg name="pressed" type="b" direction="in"/>
      <arg name="delivered" type="b" direction="out"/>
    </method>

    <!-- Feature keys: (keyCode, pressed) strokes in order, each checked
         exactly as key() checks one. Stops at the first stroke that is not
         delivered and returns how many were; a refusal of the first stroke is
         an error, as from key(). A batch that stops early does not release
         the keys its delivered strokes pressed: the caller knows which they
         are and releases them. More than 256 strokes answer
         org.freedesktop.DBus.Error.InvalidArgs. -->
    <method name="keys">
      <arg name="strokes" type="a(ub)" direction="in"/>
      <arg name="delivered" type="u" direction="out"/>
    </method>

    <!-- Feature waitForSettle: replies once the window (any window, for an
         empty id) has committed damaged content after the agent's last input
         (pointer, keys, focusWindow, raiseWindow) - or after this call, when
         no input is waiting for a settle - and then stayed quiet for quietMs.
         A commit that damages nothing, a client only asking for its next
         frame, does not count. settled is false at timeoutMs (clamped to
         30 s), when the window closes, and for every pending wait when the
         session stops or the plugin unloads; at once when no session is
         running or windowId names no usable window. elapsedMs is the time
         the call waited. A delayed reply: the compositor never waits on it.
         Locking answers the call, and every pending wait,
         org.synara.ComputerUse.Error.SessionLocked. More than 16 waits in
         flight answer org.freedesktop.DBus.Error.LimitsExceeded, and a wait
         the plugin could not schedule org.freedesktop.DBus.Error.Failed. -->
    <method name="waitForSettle">
      <arg name="windowId" type="s" direction="in"/>
      <arg name="quietMs" type="u" direction="in"/>
      <arg name="timeoutMs" type="u" direction="in"/>
      <arg name="settled" type="b" direction="out"/>
      <arg name="elapsedMs" type="u" direction="out"/>
    </method>

    <!-- Capture. PNG bytes; maxDimension 0 keeps native pixels. Failures are
         errors, never an empty array. -->
    <method name="captureWindow">
      <arg name="windowId" type="s" direction="in"/>
      <arg name="maxDimension" type="u" direction="in"/>
      <arg name="png" type="ay" direction="out"/>
    </method>
    <method name="captureRegion">
      <arg name="x" type="i" direction="in"/>
      <arg name="y" type="i" direction="in"/>
      <arg name="width" type="u" direction="in"/>
      <arg name="height" type="u" direction="in"/>
      <arg name="maxDimension" type="u" direction="in"/>
      <arg name="png" type="ay" direction="out"/>
    </method>

    <!-- Feature captureEx: the captures above with flags, and the bytes'
         MIME type as a second reply argument. Flags: 1 passive (an
         observer's frame: not agent activity, so the idle deadline and the
         badge are untouched), 2 JPEG quality 85 ("image/jpeg"), 4 raw 8-bit
         luma ("image/x-luma8; width=<w>; height=<h>", row-major, unpadded;
         outranks 2). Without 2 or 4 it is a PNG at fast compression
         ("image/png"). Unknown bits are ignored. Errors are the version 1
         methods'. -->
    <method name="captureWindowEx">
      <arg name="windowId" type="s" direction="in"/>
      <arg name="maxDimension" type="u" direction="in"/>
      <arg name="flags" type="u" direction="in"/>
      <arg name="image" type="ay" direction="out"/>
      <arg name="mime" type="s" direction="out"/>
    </method>
    <method name="captureRegionEx">
      <arg name="x" type="i" direction="in"/>
      <arg name="y" type="i" direction="in"/>
      <arg name="width" type="u" direction="in"/>
      <arg name="height" type="u" direction="in"/>
      <arg name="maxDimension" type="u" direction="in"/>
      <arg name="flags" type="u" direction="in"/>
      <arg name="image" type="ay" direction="out"/>
      <arg name="mime" type="s" direction="out"/>
    </method>

    <!-- A running session ended: request, idle-timeout, user-release, or
         session-locked. -->
    <signal name="sessionStopped">
      <arg name="reason" type="s"/>
    </signal>
  </interface>
</node>
