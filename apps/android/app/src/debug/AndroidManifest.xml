<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only additions. Merged into the release manifest by nothing: this source set is not
     part of a release build, so GalleryActivity cannot ship. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name="com.synara.android.debug.GalleryActivity"
            android:exported="true"
            android:label="Synara UI gallery"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />
    </application>
</manifest>
